[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "vortex-rdflib"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
  "rdflib>=7,<8"
]

[tool.maturin]
module-name = "vortex_rdflib.vortex_rdf_native"
python-source = "."