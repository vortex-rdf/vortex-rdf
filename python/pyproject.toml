[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "vortex-rdf-python"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "rdflib>=7.0"
]

[tool.maturin]
module-name = "vortex_rdf_python"
bindings = "pyo3"